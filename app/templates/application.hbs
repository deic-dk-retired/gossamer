{{#if session.isAuthenticated}}
  {{notification-container position="top-right"}}
  <div class="pusher">
    <div class="full height">
      <div class="toc">
        {{side-menu username=username kind=usertype}}
      </div>
      <div class="article">
        {{top-menu
          isUser=true
          routename=currentRouteName
          kind=usertype
        }}
        {{outlet}}
      </div>
    </div>
  </div>
{{else}}
  {{render "login"}}
{{/if}}
