{{#if (not-eq currentRouteName "login")}}
  {{top-menu pagetitle=currentRouteName username='Ashokaditya' usrtype='netadmin'}}
  {{side-menu}}
  <div class="appcontent">
    <div class="centered">
      {{outlet}}
    </div>
  </div>
{{else}}
  {{#if (eq currentRouteName "login")}}
    {{login}}
  {{else}}
    {{not-found}}
  {{/if}}
{{/if}}